🍵 Telegram-бот для интернет-магазина на Wildberries
Этот бот предоставляет удобный интерфейс для взаимодействия с клиентами через Telegram:
просмотр ассортимента, оформление заказов, оставление и редактирование отзывов,
а также полноценную админ-панель для управления товарами и модерации.

📦 Функционал
Для клиентов:
🍵 Просмотр списка товаров (с фото, описанием, весом и ценой)
⭐ Оставление отзыва с фото и контактом (опционально)
✏️ Редактирование своего последнего отзыва
📦 Просмотр отзывов по товарам с пагинацией
🛒 Оформление заказа → выбор товара → выбор ПВЗ (пункта выдачи)
🔗 Прямая ссылка на магазин на Wildberries
💬 Отправка предложений или обращений администратору
Для администратора:
ℹ️ Добавление новых товаров (с фото, названием, весом, описанием, ценой)
🛒 Редактирование существующих товаров (по одному полю)
🗑 Удаление товаров и автоматическое удаление связанных отзывов
⭐ Просмотр и удаление отзывов пользователей
📊 Статистика: количество активных пользователей, выручка, продажи по товарам
🔔 Автоматические уведомления об новых отзывах и товарах
🛠 Технологии
Язык: Python 3.12+
Фреймворк: aiogram 3.x
База данных: PostgreSQL (с поддержкой ON DELETE CASCADE)
Хранилище FSM: MemoryStorage (для разработки) / RedisStorage (для production)
Развертывание: поддержка polling (локально) и webhook (в облаке)
Безопасность: проверка ADMIN_ID, валидация ввода, параметризованные SQL-запросы
⚙️ Установка и запуск
1. Клонируйте репозиторий
2.Cоздайте и активируйте виртуальное окружение: python -m venv venv source venv/bin/activate
3. Установите зависимости  pip install -r requirements.txt
3. Настройте переменные окружения
Создайте файл .env:

env
12345678910111213
# Обязательно
TOKEN=YOUR_TELEGRAM_BOT_TOKEN
DATABASE_URL=postgresql://user:password@localhost:5432/dbname
ADMIN_ID=123456789
CDEK_CLIENT_ID=Id клиента CDEK
CDEK_CLIENT_SECRET=TOKEN клиента CDEK
# Для webhook (опционально)
WEBHOOK_DOMAIN=yourdomain.com
WEBHOOK_PATH=/webhook
WEBHOOK_SECRET=your_strong_secret_token
REDIS_URL=redis://localhost:6379/0  # для FSM в продакшене

💡 ADMIN_ID — ваш Telegram ID (узнать через @userinfobot)

4. Инициализация базы данных
При первом запуске таблицы создаются автоматически, но убедитесь, что PostgreSQL запущен и DATABASE_URL корректен.

5. Запуск
В режиме разработки (polling):
bash
1
python main.py
В режиме production (webhook):
Убедитесь, что заданы WEBHOOK_DOMAIN и, желательно, REDIS_URL.
Запустите:

bash
1
python main.py
Бот автоматически перейдёт в webhook-режим.

🔌 Webhook должен быть доступен по HTTPS. Используйте reverse proxy (Nginx, Caddy) или облачные сервисы (Render, Railway, Heroku + Cloudflare).

🔐 Безопасность
Все SQL-запросы используют параметризацию → защита от инъекций.
Webhook поддерживает secret_token для проверки подлинности запросов от Telegram.
Доступ к админ-панели строго по ADMIN_ID.
FSM-данные не сохраняют чувствительную информацию.

📝 Авторы проекта
1:Меличев Денис.

🙌 Поддержка
Если у вас есть вопросы или предложения — создайте issue или свяжитесь с автором.

🍵 Удачных продаж и довольных клиентов!